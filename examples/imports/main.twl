// Pull in everything geometry.twl declares, then use it like it was written
// right here. The path is relative to this file; the .twl is optional.
import "geometry";

class Main {
    void main() {
        var p = new Point(3, 4);
        print(p.sum());        // 7
        print(area(5, 6));     // 30
    }
}
